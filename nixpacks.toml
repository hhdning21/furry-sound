[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip", "ffmpeg", "libsndfile"]

[phases.install]
cmds = [
  "npm ci --prefix backend",
  "pip3 install --break-system-packages --no-cache-dir -r backend/requirements.txt"
]

[start]
cmd = "npm start --prefix backend"
